<h1>Listing button_images</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @button_images.each do |button_image| %>
  <tr>
    <td><%= link_to 'Show', button_image %></td>
    <td><%= link_to 'Edit', edit_button_image_path(button_image) %></td>
    <td><%= link_to 'Destroy', button_image, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Button image', new_button_image_path %>
